# Miya 小手机前端：初步提示词与流式链路审查

- **审查日期**：2026-08-08（星期六）
- **审查对象**：`/workspace/miya-main.zip`
- **解压目录**：`/workspace/miya-main/miya-main/`
- **审查性质**：快速静态审查，用于周日交给 Roo 继续做完整审计与二改
- **本次未做**：未实际启动网页、未连接 API、未修改项目源码、未做完整调用链和全量提示词审计

---

## 一、用户目标

Miya 是“模拟手机界面”的酒馆衍生前端，不是 RikkaHub 那类传统通用 AI 客户端。用户希望：

1. 保留小手机 UI、多气泡、引用、语音、图片、朋友圈等玩法；
2. 保留 `miyavoice`，可将其改造成自定义的模型自述/附加状态区；
3. 默认以**模型本体**交流，不在用户未要求时强制进入角色扮演；
4. 用户需要角色扮演时，再自行添加 char、联系人资料、世界书或明确指令；
5. 暴露默认提示词设置、最终请求结构和实际发送给模型的 `messages` 预览；
6. 当前阶段不准备强行给主聊天增加流式，优先保持稳定。

---

## 二、最初黑箱异常

空配置测试条件：

- 未设置角色资料；
- 未设置世界书；
- 未设置自定义提示词；
- 第一条消息仅为 `test test`。

普通通用客户端中，模型通常会回复“收到”“连接正常”或询问是否在测试接口；Miya 中的模型却表现为熟人即时通讯语境，例如询问“你在干啥”。

这说明前端在用户消息之外，默认注入了：

- 联系人/真人身份；
- 熟人关系和线上私聊场景；
- 真人微信式表达；
- 特定输出协议。

之后通过 Claude 对约束结构的概括缩小范围，再在源码中找到相应实现。该排查路径为：

```text
空配置黑箱测试
→ 观察身份与语气异常
→ 让模型概括约束类别
→ 使用关键词静态搜索源码
→ 定位动态 system 构建函数
```

---

## 三、核心结论

### 3.1 项目明确存在内置提示词

如果“没有内置提示词”指“前端不会在用户未配置时主动发送预设 system 指令”，则该说法与当前源码不符。

项目并非通过单一的 `SYSTEM_PROMPT` 常量实现，而是通过多个函数动态拼装，包括但不限于：

```text
【对话模式·线上单聊】
【全局提示词】（若存在）
用户身份与关系
联系人档案
世界书
线上格式规则
思维链规则
运转规则
能力开关
本轮格式提醒
```

动态拼装仍然属于内置提示词，而且部分规则被刻意置于接近生成端的位置。

### 3.2 必要的“小手机通信协议”和不必要的“角色身份协议”被绑定

应区分两类规则：

#### A. 小手机正常工作需要的协议

- 用户连续消息以 ` / ` 分隔；
- assistant 正文每行拆成一个气泡；
- `引用-`、`语音-`、`图片-`、`位置-`、`转账-` 等特殊格式；
- `<miyavoice>...</miyavoice>` 附加状态块。

这些规则可以保留。

#### B. 作者预设的角色/性格协议

当前默认规则包括类似内容：

- “你正在以联系人身份进行二人私聊”；
- “你是真实存在的人”；
- “你并非在扮演”；
- 默认认定模型知道用户是谁以及双方关系；
- “以温柔为底色”；
- 禁止凶狠、评判、威胁、控制、辱骂用户。

这些规则不是 UI 技术要求，会与用户自己的 char 直接冲突，应删除、改成条件式，或至少暴露为可配置预设。

### 3.3 当前默认封装不适合直接与模型本体聊天

即使没有人物卡，模型仍会被要求：

- 作为联系人/真人说话；
- 维持熟人即时通讯语境；
- 遵循角色关系；
- 执行 `<thinking> → 正文 → <miyavoice>` 三段式；
- 服从作者预设的温和表达边界。

因此，默认行为不是“模型本体 + 小手机 UI”，而是“默认联系人角色 + 小手机 UI”。

---

## 四、已定位的主要源码

### 4.1 主聊天请求和提示词拼装

文件：

```text
js1/miya-chat-engine.js
```

重点函数/区域：

```text
getGlobalPrompt()
buildChatModeBlock()
buildThinkingRulesHead()
getThinkingRulesFormatTailItems()
buildThinkingRules()
appendOnlineThinkingRulesMessage()
buildOperationRulesHead()
getOperationRulesFormatTailItems()
buildOperationRules()
buildPromptDebug()
buildPromptSourceBreakdown()
appendHistoryToApiMessages()
stripThinkingForApi()
parseThinking()
extractThinkingFromResponse()
```

核心身份规则主要位于：

```text
buildChatModeBlock()
buildOperationRulesHead()
```

核心 thinking 规则主要位于：

```text
buildThinkingRulesHead()
getThinkingRulesFormatTailItems()
```

### 4.2 线上气泡与三段式协议

文件：

```text
js1/miya-chat-online-format.js
```

重点内容：

- `<thinking> → 正文 → <miyavoice>`；
- 每行一条气泡；
- 每轮格式提醒；
- 引用、语音、图片、旁白等单行协议；
- 防止 thinking/miyavoice 标签泄漏进正文。

重点函数/关键词：

```text
buildPerTurnFormatReminder()
【输出顺序】
【本轮格式】
【格式示例】
```

### 4.3 自定义思维链预设

文件：

```text
js1/miya-chat-thinking-rules.js
```

该模块允许用户自定义“思维链风格指引”，但会自动追加硬性格式要求；它不是关闭 thinking 的开关。

界面说明中明确写了：

```text
此处写的是给模型的思维链风格指引（system 提示），
不是角色要发出去的聊天正文。
```

### 4.4 自定义运转规则

文件：

```text
js1/miya-chat-operation-rules.js
```

需要继续确认：

- 哪些默认规则可被自定义覆盖；
- 哪些格式/行为规则始终强制追加；
- 自定义预设是否仍无法消除身份与温柔限制。

### 4.5 `miyavoice` 模板

文件：

```text
js1/miya-chat-heartvoice-templates.js
```

目前默认心声字段偏角色扮演，例如：

```text
好感度
欲望值
行为动作
角色心声
```

该机制可以保留，并通过自定义模板改造成较中性的模型附加说明，例如：

```text
当前理解
判断
不确定点
补充倾向
```

### 4.6 聊天设置与存储

文件：

```text
js1/miya-chat-store.js
js1/miya-chat-contact-settings.js
js1/miya-chat-global-settings.js
js2/miya-settings-app.js
```

需要继续审计 effective settings（最终生效设置）如何由默认值、全局值、联系人值合并。

---

## 五、默认思维链规则的实际用途

主聊天默认注入的思维链规则大意为：

```text
【思维链·必读】
你是某联系人，须在 <thinking> 标签内完成回复前的内部思考。
须消化人设与世界书、把握与用户的关系及当下情绪，
回顾近期已发内容以避免重复话题和同质化描写，
并规划本轮正文气泡与格式。
思维链只写在该段，不得写入正文。
```

因此它不是单纯为了展示模型推理，而是模型侧的：

- 人设与世界书检查区；
- 关系和情绪判断区；
- 防重复检查区；
- 气泡和格式规划区。

Miya 的处理流程大致为：

```text
API 的 reasoning_content / reasoning
或 content 内的 <thinking>
→ 提取为 thinking
→ 普通气泡中剥离
→ 保存到 activeThinking 等状态
→ 历史 assistant 内容再次发给 API 时通常剥离 thinking
```

相关代码：

```text
extractReasoningFromApi()
extractThinkingFromResponse()
parseThinking()
stripThinkingBlocks()
stripThinkingForApi()
```

当前用户倾向：不必优先改 thinking 解析链；`miyavoice` 可以承担可见的自定义自述。首要问题是默认角色扮演协议，而非 thinking 是否展示。

---

## 六、多气泡在模型侧的实际表示

代码中存在：

```js
var USER_MSG_JOIN = ' / ';
```

连续用户气泡会被合并成一个 API `user` message：

```json
{
  "role": "user",
  "content": "第一条 / 第二条 / 第三条"
}
```

历史中连续的 assistant 气泡则会以换行合并：

```json
{
  "role": "assistant",
  "content": "第一条\n第二条\n第三条"
}
```

因此：

```text
用户气泡边界：" / "
模型气泡边界：换行
```

对模型而言不需要美化，只要角色、边界和 JSON 结构稳定即可。该方案简单、省 token，可以保留。

---

## 七、`system_lead` 与全局提示词入口

### 7.1 `system_lead` 只是统计分类

源码中：

```js
system_lead: '前置系统指令'
```

位于 `PROMPT_SOURCE_LABELS`，用于提示词来源分类和 token 统计，并不能证明存在一个对应的可见输入框。

分类逻辑大意是：如果最终 API messages 中第一条是较短的 system 消息，且不是内置运转规则，就标为“前置系统指令”。

### 7.2 底层存在全局提示词存储键

存储键：

```text
miya-global-break-prompt
```

读取位置包括：

```text
js1/miya-chat-engine.js
js1/miya-chat-group.js
js1/miya-appointment-engine.js
js2/miya-settings-app.js
```

聊天引擎会把它包装为：

```text
【全局提示词】
用户保存的内容
```

但本次快速搜索只明确发现了读取、备份和注入逻辑，尚未找到当前版本中正常可见的编辑 textarea/保存入口。可能是：

- 旧版本遗留字段；
- UI 后来被移除；
- 依赖导入旧存档；
- 当前版本遗漏了编辑入口。

需要 Roo 进一步确认。

即使能够写入该全局提示，也不适合用它强行覆盖角色协议，因为后续还会追加更靠近生成端的思维链和运转规则，容易产生 system 内部冲突。

---

## 八、流式能力初步审查

### 8.1 PWA/网页前端本身可以流式

不存在“网页版/PWA 天生不能流式”的问题。项目本身已有实际流式实现。

### 8.2 主聊天当前默认非流式

文件：

```text
js1/miya-chat-engine.js
```

当前主聊天请求大致为：

```text
fetch(...)
→ r.json()
→ 等完整回复
→ 解析 thinking / 正文 / miyavoice / 特殊指令
→ 拆气泡、保存、触发后处理
```

主请求体当前未见 `stream: true`，也未在该链路读取 `response.body`。

### 8.3 预约/线下模块已有完整流式实现

文件：

```text
js1/miya-appointment-engine.js
js1/miya-appointment-app.js
```

已有能力：

- `stream: true / false`；
- `response.body.getReader()`；
- `TextDecoder`；
- SSE `data:` 解析；
- `choices[0].delta.content`；
- `reasoning_content` / `reasoning`；
- `onDelta` 增量回调；
- 流式和非流式兼容。

多个 deep/bridge 模块中也存在流式实现。

### 8.4 主聊天暂时保持非流更保险

主聊天完整回复后还要处理：

- thinking；
- miyavoice；
- 气泡拆分；
- 特殊标签与指令；
- 记忆和 token 统计；
- 主动推送/life-like；
- 持久化、通知等。

强行逐 token 跑全部后处理容易误解析半截标签或频繁重绘。技术上可以做成“流式临时展示，结束后统一正式解析”，但当前用户仅需确认可行性，不打算立即改造。建议主聊天继续非流。

### 8.5 Service Worker 暂非主要障碍

`sw.js` 主要拦截 GET 缓存；主聊天是 POST，因此当前聊天请求不会被该缓存路径直接拦截。

---

## 九、建议的默认提示词调整方向

不需要新增复杂的“角色模式/模型模式”分支。可直接把默认封装改为中性：

```text
默认以模型自身身份交流；
只有用户当前请求、联系人档案、世界书或其它明确资料指定角色时，
才遵循相应身份和背景。
联系人名称、头像和 UI 壳本身不应自动构成角色扮演指令。
```

### 保留

- ` / ` 用户气泡分隔；
- assistant 换行拆气泡；
- 引用、语音、图片等小手机协议；
- `miyavoice`；
- 世界书和联系人资料的注入能力；
- 当前非流式主聊天；
- 现有存储、备份和玩法。

### 删除或改成条件式

- “你是联系人”；
- “你是真实存在的人”；
- “你并非在扮演”；
- 无资料时仍强制双方关系；
- “温柔为底色”；
- 禁止凶狠/辱骂/控制等与 char 可能冲突的作者默认价值偏好；
- 无设定时仍要求消化人设与世界书。

### 可考虑的中性默认文案

```text
【默认身份】
默认以你自身的模型身份与用户交流，不自行假定额外角色、人格、现实身份、
双方关系或共同经历。若用户请求、联系人档案、世界书或其它明确资料指定了
身份和背景，则遵循这些设定；若未指定，则保持模型自身身份。

【小手机通信协议】
用户连续发送的多条消息以「 / 」分隔，该符号仅表示消息边界。
回复正文每行会显示为一个独立气泡。
按当前功能约定输出引用、语音、图片等特殊行。
正文末尾按当前模板输出 <miyavoice>，该段不进入普通聊天气泡。
```

---

## 十、透明度改造建议

### 10.1 暴露最终生效设置

展示当前聊天经默认值、全局设置、联系人设置合并后的 effective settings，而不只是各页面零散原始值。

### 10.2 暴露最终 `messages` 数组

用户应能直接看到实际送往 API 的内容：

```json
[
  { "role": "system", "content": "..." },
  { "role": "user", "content": "..." },
  { "role": "assistant", "content": "..." }
]
```

需要包括：

- role；
- 实际顺序；
-完整 content；
- 动态本轮提醒；
- `/` 合并后的用户消息；
- 换行合并后的 assistant 历史。

### 10.3 同时提供结构视图

建议按来源折叠展示：

```text
系统主提示
全局提示词
对话模式
联系人档案
世界书
思维链规则
运转规则
本轮格式提醒
历史消息
当前用户输入
```

每项至少显示：

- role；
- 顺序；
- 来源分类；
- 字符数/token 估算；
- 可展开全文。

### 10.4 请求前保存快照

项目已有：

```text
buildPromptDebug()
buildPromptSourceBreakdown()
lastPromptDebug
lastPromptMeta
```

但现状更像生成成功后的调试记录。建议在 `fetch()` 前保存完整请求快照，这样即使 API 报错也能查看本轮到底发送了什么。

注意不要把 API key 放进预览；仅保存/展示 `built.messages`、必要请求参数和有效设置。

---

## 十一、周日交给 Roo 的建议任务清单

### P0：完整提示词审计

1. 从主聊天发送入口追踪到 `built.messages`；
2. 列出每条 system/user 隐藏注入的顺序、来源和触发条件；
3. 确认生成前最后几条 message 的内容；
4. 搜索所有可能的角色与价值偏好硬规则：

```text
你是
真实存在
并非在扮演
线上单聊
真人聊天
关系
温柔
辱骂
凶狠
凶很
威胁
控制
评判
必须
禁止
硬性
```

5. 不只审计主聊天，还要标记群聊、通话、预约等是否各自有重复规则。

### P0：请求预览

1. 在实际请求发送前保存 `built.messages`；
2. 增加“本轮实际请求”入口；
3. 提供结构视图和原始 JSON 视图；
4. 请求失败时仍保留快照；
5. 显示 effective settings。

### P1：中性化默认提示词

1. 保留小手机格式协议；
2. 默认改为模型本体；
3. 有明确 char/世界书/用户指令时再扮演；
4. 删除“温柔为底色”“不许骂用户”等与 char 冲突的硬性人格偏好；
5. 检查所有后置提醒，防止改完主规则后又被别处重新注入。

### P1：确认自定义预设的真实覆盖范围

检查：

```text
js1/miya-chat-operation-rules.js
js1/miya-chat-thinking-rules.js
js1/miya-chat-heartvoice-templates.js
```

明确：

- 哪些内容可替换；
- 哪些内容始终追加；
- 哪些 UI 文案容易让用户误以为已经覆盖全部默认规则。

### P2：全局提示词入口

确认 `miya-global-break-prompt`：

- 当前是否存在隐藏/动态 UI；
- 是否只是遗留字段；
- 是否需要恢复成明确的“前置 system 指令”输入框；
- 注入位置和优先级是否合理。

### P3：暂不处理主聊天流式

除非后续明确需要，否则主聊天继续非流。已有预约模块可作为未来实现参考。

---

## 十二、验收建议

二改后可进行以下黑箱测试：

### 1. 空配置本体测试

输入：

```text
test test
```

预期：识别为普通测试或中性消息，不应自动以熟人联系人身份搭话。

### 2. 身份测试

输入：

```text
你是谁？
```

预期：默认按模型自身身份回答；不应因联系人名称/头像而坚称自己是真实人类。

### 3. char 覆盖测试

给出明确的尖锐、敌对或冷漠 char。

预期：不再被“温柔为底色”“禁止辱骂”等前端默认偏好磨平；仍需服从模型服务本身的安全边界。

### 4. 无 char 不脑补测试

空资料聊天。

预期：不自行虚构双方关系、共同回忆或现实经历。

### 5. 小手机协议测试

用户连续发送三条消息。

预期：模型能理解 ` / ` 仅为气泡边界；assistant 仍可用换行输出多个气泡。

### 6. `miyavoice` 测试

使用自定义模板。

预期：正文与 `miyavoice` 正确分离，附加字段可查看且不泄漏进普通气泡。

### 7. 请求透明度测试

在请求发送前、成功后、失败后分别检查预览。

预期：用户均能看到准确的最终 `messages` 和有效设置。

---

## 十三、最终判断

Miya 的价值在于“小手机”交互和丰富玩法，不需要被改造成 RikkaHub 式传统客户端。当前最合适的二改方向是：

```text
保留小手机通信协议和 miyavoice
+ 默认模型本体
+ 有明确资料时才角色扮演
+ 暴露最终 prompt/messages
+ 删除会覆盖 char 的作者人格硬规则
```

本次审查已经确认：

- 网页/PWA 技术上能够流式；
- Miya 自身已有多处流式实现；
- 主聊天目前非流式，保持现状更稳；
- 主聊天存在大量动态内置提示；
- 当前默认身份确实是强角色化联系人，而非中性模型本体；
- “温柔、不辱骂、不控制”等后置硬规则会与用户 char 冲突；
- `system_lead` 是统计分类，不等于可见输入框；
- 底层存在 `miya-global-break-prompt`，但当前可见编辑入口尚未找到；
- 项目已有 prompt 调试/来源统计基础，可用于实现请求透明化。

---

## 十四、聊天时间语义与协议清洗最终记录

### 14.1 本批最终范围

- 新消息在实际写入边界固定浏览器本地 `createdAt`，使用 `YYYY/MM/DD-星期X-HH:mm:ss` 作为界面显示和模型逐条前缀的统一格式；
- 新实时真实消息在写入边界固定 `conversationGap`，严格大于 30 分钟时保存跨段原始时差；
- 跨段界面分隔只读取已保存的 `conversationGap`，不受逐条时间显示开关影响；普通用户请求只注入一次跨段时长提醒，主、备用 API 按 2xx 成功状态共同消费同一次提醒；
- 单聊和群聊按逐候选消息分配时间，解析类型协议后保存时间保持一致；系统消息和线下技术消息不参与有效会话锚点计算；
- 协议清洗按来源处理：`user` / `system` 的合法方括号和旧 `⧗` 正文保持原样；`assistant` 只清理开头旧协议，以及与该消息自身时间完全匹配的新前缀，正文中间或不同时间的文本保留；该行为覆盖 `callLine`、单行多类型和群聊链路；
- appointment 历史和镜像缺失时间时保持 `0`，未知时间稳定置后；只有真实创建边界赋当前时间；
- 旧历史不回填 `createdAt` 或 `conversationGap`，避免把读取时刻伪装成原始消息时间。

### 14.2 关键决策

1. **固定事实，不在读取时推算**：消息时间与跨段时差均在真实写入边界确定，后续界面和模型只消费已保存字段。
2. **严格跨段阈值**：只有间隔 `> 30` 分钟才创建跨段语义，恰好 30 分钟不触发。
3. **来源感知正文保护**：仅删除能由消息来源和自身时间确认的协议前缀，不用宽泛正则清理用户正文。
4. **未知历史保持未知**：旧记录缺失时间时不以当前时间补齐，并维持可预测的稳定排序。

### 14.3 实际验证

- 对本批所有实际修改的 JavaScript 文件分别执行 `node --check`；
- 执行 `git diff --check` 检查差异空白和冲突标记；
- 完成独立代码审查，覆盖固定时间、跨段提醒消费、类型协议和来源感知清洗链路；
- 用户已明确报告两阶段浏览器验收通过：第一阶段验证主要时间与跨段流程，第二阶段验证协议清洗和相关边界；
- 未执行完整自动化测试、完整端到端测试或联网 API 测试。

### 14.4 已知边界

- 历史中已经落盘的伪时间与真实时间没有可靠来源标记，无法无损区分并自动恢复；
- 系统时区变化会改变同一时间戳对应的本地墙上时间显示；
- 页面内跨段提醒的消费状态不跨页面重载持久化，重载后的行为以已保存消息状态和当次页面会话为准。
